description: Message sending test
label: userTest
input:
  triggeredBy: sendMessage()
outputMessage:
  sentTo: userExchange
  body:
    user: Alexandre tavares
  headers:
    contentType: application/json
